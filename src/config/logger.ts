// A placeholder for a logging service
const logger = {
  fatal: console.error,
  error: console.error,
  warn: console.warn,
  info: console.info,
  debug: console.log,
  trace: console.log,
};

export default logger;
